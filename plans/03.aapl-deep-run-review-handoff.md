# AAPL Deep Run Review Handoff

## Objective

Continue from the completed review of the latest deep AAPL equity run. No code changes have been made.

## Runs

- Reviewed: `data/runs/2026-07-01T05-39-32-201Z-f545c341/`
- Comparable baseline: `data/runs/2026-06-30T14-28-13-211Z-7ee762a6/`
- Calibration: `data/calibration/summary.json`

The baseline matches `jobType=equity`, `assetClass=equity`, symbol `AAPL`, depth `deep`, and five-trading-day prediction horizons.

## Complete Review

1. **Prediction shortfall regressed**
   - **Symptom:** Forecasts fell from 4/5 to 2/5.
   - **Evidence:** `analytics.json:predictions.count=2,targetCount=5,missingCount=3`; baseline `count=4,missingCount=1`. New run has only `range` and `relative`, both five-day.
   - **Suspected cause:** Forecast selection overreacted to weak calibration: `report.json:dataGaps[5]` says remaining shapes were suppressed because calibration was negative and probabilities were near 0.5. No replacement was attempted: `analytics.json:predictions.replacementAttempted=false`.
   - **Severity:** High. **Effort:** Medium.

2. **Calibration policy relies on a very small sample**
   - **Symptom:** Negative equity calibration materially constrains forecast generation despite only five resolved equity predictions.
   - **Evidence:** `analytics.json:calibrationAtGeneration.assetClass={count:5,brierScore:0.2591,brierSkillScore:-0.0364}`. Current `data/calibration/summary.json:byJobType.equity` remains `count=5,brierScore=0.2591`.
   - **Suspected cause:** Forecast-selection logic lacks a minimum-sample or uncertainty threshold before applying negative skill as a strong suppression signal.
   - **Severity:** High. **Effort:** Medium.

3. **Prediction-shortfall explanation is internally incorrect**
   - **Symptom:** The report says “A fifth prediction was not emitted,” although three predictions are missing.
   - **Evidence:** `report.json:dataGaps[5]` versus `report.json:dataGaps[17]="predictionShortfall: emitted 2 of 5..."` and `analytics.json:predictions.shortfall.missingCount=3`.
   - **Suspected cause:** Model-authored narrative was not reconciled with deterministic shortfall telemetry.
   - **Severity:** Medium. **Effort:** Low.

4. **Weak-evidence language escaped final synthesis**
   - **Symptom:** A bearish claim states that price increases could expose weak demand elasticity without an explicit uncertainty posture.
   - **Evidence:** `trace.json:postSynthesisAudit.warnings[0]` reports `weak-evidence-posture-missing` at `bearCase[1]`. This warning was absent from the baseline.
   - **Suspected cause:** Critique/final synthesis did not apply the evidence-posture convention to a claim built partly from reused web evidence.
   - **Severity:** Medium. **Effort:** Low.

5. **Supplemental coverage remains configuration-constrained**
   - **Symptom:** Derivatives volatility and supplemental market lanes remain uncovered; earnings implied move is unavailable.
   - **Evidence:** `normalized/evidence-lanes.json:lanes[derivatives-volatility].status="gap"` due to missing `MARKET_BOT_TRADIER_API_TOKEN`; `lanes[supplemental-market].status="gap"` due to the Massive plan. Coverage remains `9/11`, identical to baseline.
   - **Suspected cause:** Local credentials/provider plan, not synthesis behavior. Core and material lanes remain covered.
   - **Severity:** Medium. **Effort:** Low for Tradier configuration; external cost for Massive.

6. **Web evidence was reused, not refreshed**
   - **Symptom:** The run cites nine reused-profile web sources but gathered no fresh web evidence.
   - **Evidence:** `analytics.json:webSources=null`; `reusedProfileWebSources={accepted:11,reportCited:9,generatedAt:"2026-06-28T10:42:57.445Z",ageDays:2}`; `normalized/web-subject-profile.json:openGaps` still contains five unresolved areas.
   - **Suspected cause:** Profile reuse TTL accepted the prior profile, so the web-gather stage did not refresh it.
   - **Severity:** Low. **Effort:** Medium.

7. **No action: integrity controls passed**
   - All 42 uniquely cited source IDs exist in `report.json:sources`.
   - `score.json:scores[*].status="pending"` because the five-day horizons have not elapsed; these are not misses, and no `miss-autopsy.json` is expected.
   - `normalized/source-gaps.json` has no duplicate `(source,message)` pairs; the baseline’s duplicate SEC `grossProfit` gap was removed.

## Constraints

- Research-only boundary applies; do not introduce buy/sell/hold, sizing, or execution language.
- Predictions must remain publicly observable and scoreable.
- Bun + oxc only.
- Do not add speculative abstractions or unrelated refactors.
- If code is changed, add tests and run `bun run check`.

## Suggested Skills

- `coding-principles` for surgical implementation work.
- `explore-codebase` to locate forecast selection, calibration-context, deterministic shortfall, and post-synthesis audit seams.
- `javascript-testing-patterns` when adding regression tests.
- `requesting-code-review` after implementation.
- `run-review` after generating a new comparable AAPL run to verify improvement.

## Recommended Next Focus

Start with findings 1–3 as one bounded investigation:

- determine where negative calibration influences model prompt or forecast acceptance;
- add a minimum resolved-sample policy if supported by existing calibration conventions;
- ensure deterministic shortfall text cannot conflict with model-authored data gaps;
- preserve the soft-target rule rather than padding forecasts.
